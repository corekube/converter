apiVersion: v1
kind: ConfigMap
metadata:
  name: git-sync-config
data:
  git.sync.repo: "https://github.com/corekube/web"
  git.sync.branch: "dev"
  git.sync.rev: "origin/dev"
  git.sync.dest: "/srv/data/stage/src"
  git.sync.wait: "45"
